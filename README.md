# Статичный сайт «Студии керамики для школ и детских садов»

Это обычный HTML/CSS/JS-сайт, не привязанный к Tilda.

## Страницы

- `index.html` — главная
- `detskiy-sad.html` — страница детского сада
- `shkola.html` — страница школы
- `tech.html` — технические требования

## Фото и логотипы

Фотографии и логотипы нужно скачать из архива:
https://disk.yandex.ru/d/aRTiLwVQHPqh8w

После скачивания положите папки с фотографиями рядом с HTML-файлами, чтобы структура была такой:

```text
index.html
detskiy-sad.html
shkola.html
tech.html
assets/
logos/
gardens/
garden/
schools/
```

В HTML прописаны относительные пути из ТЗ, включая кириллицу, пробелы и спорные места исходного документа:

- `gardens/garden_300 Фото1.jpg`
- `garden/garden300 Фото2.jpg`
- `schools/school_15000 Фото27.jpg`
- `schools/school_1500  Фото28.jpg` — с двумя пробелами перед `Фото28.jpg`

Если фактические имена файлов в архиве отличаются, переименуйте файл или поправьте `src` в HTML.

## Формы

Формы отправляются через FormSubmit на почту `info@razvitiekeramiki.ru`.

После первого реального отправления FormSubmit пришлет письмо для подтверждения адреса. Пока адрес не подтвержден, сервис будет отправлять письма активации, а затем начнет пересылать заявки на почту.

## Запуск локально

Просто откройте `index.html` в браузере. Для проверки путей лучше запустить локальный сервер:

```bash
python3 -m http.server 8080
```

Затем открыть:

```text
http://localhost:8080
```

## Dev / Prod

Схема разработки и публикации через два GitHub-репозитория описана в `DEPLOYMENT.md`.
